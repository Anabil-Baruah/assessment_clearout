export const LIMIT_PER_MINUTE = Number(process.env.LIMIT_PER_MINUTE || 5);
export const COLLECTION_NAME = process.env.COLLECTION_NAME || "usage";
export const PORT = Number(process.env.PORT || 3000);
